import re

def refactor_generator():
    with open("apps/api/services/brief_generator.py", "r") as f:
        content = f.read()
    
    # Replace the initialization and _generate_with_gemini
    # Let's just rewrite the file basically, or replace the function.
    pass

# Given the complexity, let's just use sed or rewrite the specific functions
