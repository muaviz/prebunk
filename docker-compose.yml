version: '3.8'
services:
  api:
    build: ./apps/api
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/app/data

  web:
    build: ./apps/web
    ports:
      - "3000:3000"
    env_file: ./apps/web/.env.local
    depends_on:
      - api
